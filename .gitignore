.DS_Store

# moss build/runtime state — regenerated locally, never tracked
.moss/build/
.moss/identity/
.moss/data/
.moss/cache/

# git worktrees (project-local)
.worktrees/

# moss writes an agent skill file into the vault on every build
.claude/
.cursor/
